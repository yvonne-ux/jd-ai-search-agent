# Secrets — never commit
.env

# Confidential internal documents — keep local, do not push
*.docx
*.pdf

# Candidate data and generated output (PDPA — never commit)
data/*
!data/.gitkeep

# Live client mandate briefs used for testing — keep local (PDPA)
tests/KH1284_*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS
.DS_Store
